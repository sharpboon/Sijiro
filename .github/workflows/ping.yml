name: Streamlit Keep-Alive Ping

on:
  schedule:
    - cron: "*/50 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        continue-on-error: true
        run: curl -s "https://sijiro-mhc8nhmtpkslflvqonvcmv.streamlit.app/"
