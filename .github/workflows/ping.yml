name: Streamlit Keep-Alive Ping

on:
  schedule:
    - cron: "*/50 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -s "https://sijiro-mhc8nhmtpkslflvqonvcmv.streamlit.app/" > /dev/null || true
          echo "Ping sent to Streamlit App!"
